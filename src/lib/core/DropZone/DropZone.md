```jsx
const DndProvider = require('../DndProvider').default;

<DndProvider>
  <div style={{ height: 400, display: 'flex' }}>
    <DropZone>
      Drag presentations, playlists or folders here from the right pane.
    </DropZone>
  </div>
</DndProvider>;
```
