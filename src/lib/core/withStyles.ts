import { createStyles, withStyles, WithStyles } from '@material-ui/core';
import { useTheme, makeStyles } from '@material-ui/styles';
export default withStyles;
export { WithStyles, createStyles, useTheme, makeStyles };
