import { createStyles } from '../withStyles';

const styles = createStyles({
  root: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'stretch',
    minHeight: 68,
  },
});

export default styles;
