import { createStyles } from '../../core/withStyles';

const styles = createStyles({
  root: {
    flex: 1,
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'stretch',
  },
});

export default styles;
