- type: serial
  exclude: gh-pages
  steps:
  - type: parallel
    service: elements
    steps:
      - command: yarn lint
      - command: yarn test
  - type: serial
    tag: ^(master)
    service: elements
    steps:
      - command: bash deploy-npm.bash
      - command: bash deploy-gh-pages.bash
